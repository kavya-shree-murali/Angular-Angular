import { StandaloneDirective } from './standalone.directive';

describe('StandaloneDirective', () => {
  it('should create an instance', () => {
    const directive = new StandaloneDirective();
    expect(directive).toBeTruthy();
  });
});
