import { StandalonePipe } from './standalone.pipe';

describe('StandalonePipe', () => {
  it('create an instance', () => {
    const pipe = new StandalonePipe();
    expect(pipe).toBeTruthy();
  });
});
